# MyNote P1 批次 D 实施计划（1.5.0：条目 39/40/41/42/45）

> **For agentic workers:** 配套设计文档 `docs/superpowers/specs/2026-09-14-p1-batch-d-design.md`。分两条并行线：
> - **主线程（39-42）**：`ImageStore.kt`、`NoteDao.kt`（+getDeletedBefore）、`NoteRepository.kt`（+purgeNotes/purgeExpired 改造）、`TrashViewModel.kt`、`TrashScreen.kt`、`MyNoteApp.kt`、`NoteEditScreen.kt`（缩略图预览）+ 相关测试。
> - **并行子代理（45 机械部分）**：`NoteDao.kt` 全部列表查询投影化 + `NoteListItem` POJO 新增 + `NotesViewModel.kt`/`NotesScreen.kt`/`NoteRow.kt` 类型适配 + 相关测试；等主线程 39-42 落定后派发（同一文件需避免并发改写，两线文件大部分重叠时串行或严格分区）。
> - 迁移（AppDatabase + NoteEntity + 迁移测试）由主线程在子代理查询改写后统一接线。
> 步骤使用 `- [ ]` 跟踪。

**Goal:** 图片导入 IO 化、预览缩略图 + Coil 缓存上限、启动清理 SQL 化、清空回收站批量一次 GC、notes 索引与投影查询（Room v5）；发布 1.5.0。

**Tech Stack:** Kotlin、Compose、Coil 2、Room(KSP)、Robolectric/JUnit4。无新依赖、无新权限。

---

### Task 1: 条目 39 — 图片导入 IO（主线程）

**Files:** Modify `app/src/main/java/com/mynote/app/data/image/ImageStore.kt`

- [ ] `importAndCompress` 内部 `withContext(Dispatchers.IO)`（含 generateThumbnail）；签名不变。
- [ ] `ImageStoreTest` 既有用例通过（行为不变）。

### Task 2: 条目 40 — 预览缩略图 + Coil 缓存（主线程）

**Files:** Modify `NoteEditScreen.kt`、`app/src/main/java/com/mynote/app/MyNoteApp.kt`（文件已存在，先读）

- [ ] 预览 `AsyncImage`：`imageStore.thumbFile(name).takeIf { it.exists() } ?: imageStore.physicalFile(name)`。
- [ ] `MyNoteApp` 实现 `ImageLoaderFactory`（内存缓存上限约 20MB）；检查 `AndroidManifest.xml` 是否需要 `android:name`。
- [ ] 复用逻辑抽小函数（列表/预览共用）避免重复。

### Task 3: 条目 41 — 启动清理 SQL（主线程）

**Files:** Modify `NoteDao.kt`、`NoteRepository.kt`

- [ ] `getDeletedBefore(cutoff)`。
- [ ] `purgeExpiredDeletedNotes` 改用它 + `withTransaction` 批量删 + 一次 GC。

### Task 4: 条目 42 — 清空回收站批量（主线程）

**Files:** Modify `NoteRepository.kt`、`TrashViewModel.kt`、`TrashScreen.kt`

- [ ] `purgeNotes(list)`：单事务 + 一次 GC。
- [ ] `TrashViewModel.purgeAll` 改用；`purging` StateFlow；`TrashScreen` 清理中禁用并显示「清理中…」。
- [ ] 测试：NoteRepositoryTest 批量清理 + GC；TrashViewModelTest purging。

### Task 5: 条目 45 — Room v5（主线程接线 + 并行子代理机械改造）

- [ ] `NoteEntity` 索引；`AppDatabase` v5 + `MIGRATION_4_5`；迁移测试。
- [ ] （子代理）`NoteListItem` POJO + 列表查询投影化 + VM/Screen/NoteRow 适配 + 测试。
- [ ] 主线程复核集成，修复编译与断言问题。

### Task 6: 验证与发布

- [ ] 全量单测通过；版本 1.5.0 / 1_05_00；README 同步；backlog 39-42、45 → 已完成（v1.5.0）+ 修订记录。
- [ ] `assembleRelease` 产出校验；本地提交（不 push）。

---

## 修订记录

- 2026-09-14 计划创建。
- 2026-09-14 执行情况：39-42 由主线程完成（无偏差）；45 拆为「主线程迁移接线（NoteEntity 索引 + AppDatabase v5 + MIGRATION_4_5 + 迁移测试）+ 子代理投影改造（NoteListItem + 13 条列表查询 + VM/Screen/NoteRow/TrashScreen）」。两处与计划的偏差：① 子代理指出 TrashViewModel 也必须改（`StateFlow<List<NoteEntity>>` 无法接收投影），最终 `restore/purge` 改为按 id 取实体、`purgeAll` 先 `getNotesByIds` 再批量删；② 投影波及 `AiChatViewModelTest` 中对列表元素取 `.content` 的断言，改为 `.summary`。另：迁移测试中原有 3→4 用例需补 `MIGRATION_4_5`（目标版本升到 5 后必须给出完整迁移链）。