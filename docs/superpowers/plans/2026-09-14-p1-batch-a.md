# MyNote P1 批次 A 实施计划（1.4.2：条目 21/22/23）

> **For agentic workers:** 配套设计文档 `docs/superpowers/specs/2026-09-14-p1-batch-a-design.md`。文件域划分：条目 22 独立（`NoteUndoController.kt` / `NoteUndoControllerTest.kt` / `NoteEditScreen.kt`），条目 21+23 在主线程串行（`NotesScreen.kt` / `NotesViewModel.kt` / `NotesViewModelTest.kt`）——两组文件互不重叠，可并行。步骤使用 `- [ ]` 跟踪。

**Goal:** 排序文案修正、撤销连续输入合并、主页冷启动加载态；发布 1.4.2。

**Tech Stack:** Kotlin、Compose、Robolectric/JUnit4。无新依赖、无新权限、无 Room 迁移。

---

### Task 1: 条目 21 — 排序文案（主线程）

**Files:** Modify `app/src/main/java/com/mynote/app/ui/notes/NotesScreen.kt`

- [ ] `"✓ 排序：最早创建"` → `"✓ 排序：最新创建"`；`"排序：最早创建"` → `"排序：最新创建"`（仅文案，SQL/测试不变）。

### Task 2: 条目 22 — 撤销合并（独立子代理）

**Files:** Modify `app/src/main/java/com/mynote/app/ui/notes/NoteUndoController.kt`、`NoteEditScreen.kt`；`app/src/test/java/com/mynote/app/ui/notes/NoteUndoControllerTest.kt`

- [ ] `record(before, atMillis = System.currentTimeMillis(), force = false)`；合并规则见设计文档（600ms 窗口 + 文本相同或相邻单字符增删 → 替换栈顶）。
- [ ] `NoteEditScreen.kt:468` 默认调用；`:272`（AI 结果）与 `:287`（插图）传 `force = true`。
- [ ] 测试：连续插入合并 / 连续删除合并 / 窗口过期新格 / force 新格 / 非相邻不合并 / 选区-only 折叠。

### Task 3: 条目 23 — 冷启动加载态（主线程）

**Files:** Modify `app/src/main/java/com/mynote/app/ui/notes/NotesViewModel.kt`、`NotesScreen.kt`；`app/src/test/java/com/mynote/app/ui/notes/NotesViewModelTest.kt`

- [ ] `notes` 类型 `StateFlow<List<NoteEntity>?>`，初值 `null`；`selectAll()` 改 `notes.value.orEmpty()`。
- [ ] `NotesScreen`：`notes == null` 渲染「加载中…」占位；否则按空态/列表渲染；行渲染用 `notes ?: emptyList()` 不可行处改为非空分支。
- [ ] 测试适配可空断言；新增 `notesIsNullBeforeFirstEmission`。

### Task 4: 验证与发布

- [ ] 全量 `.\gradlew :app:testDebugUnitTest` 通过（记录新总数）。
- [ ] `app/build.gradle.kts`：`1.4.2` / `1_04_02`。
- [ ] README：版本行、测试数、产物名 `MyNote-1.4.2-release.apk`。
- [ ] `docs/backlog.md`：条目 21/22/23 → 已完成（v1.4.2）；修订记录追加一行。
- [ ] `.\gradlew :app:assembleRelease` 产出校验。
- [ ] 本地提交（不 push）。

---

## 修订记录

- 2026-09-14 计划创建。
- 2026-09-14 执行情况：条目 22 中「贴 AI 结果 force=true」一并实现（属撤销粒度，非 AI 助手功能本身）；条目 23 无偏差。