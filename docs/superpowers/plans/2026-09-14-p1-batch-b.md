# MyNote P1 批次 B 实施计划（1.4.3：条目 25/26/27/28）

> **For agentic workers:** 配套设计文档 `docs/superpowers/specs/2026-09-14-p1-batch-b-design.md`。文件域划分：条目 28 的渲染/导出域（`data/export/` + `ui/export/`，不含编辑页）由独立子代理并行实现；条目 25/26/27 全部落在 `NoteEditScreen.kt`（+ `ImageStore.kt` / `NoteRepository.kt` / `NoteEditViewModel.kt`），由主线程串行实现。步骤使用 `- [ ]` 跟踪。

**Goal:** 编辑页焦点链完善、全屏预览接线、插图失败反馈与图片回收时机、导出/分享缺图提示；发布 1.4.3。

**Tech Stack:** Kotlin、Compose、Room、Robolectric/JUnit4。无新依赖、无新权限、无 Room 迁移。

---

### Task 1: 条目 28 — 渲染/导出域（独立子代理）

**Files:** `app/src/main/java/com/mynote/app/data/export/`（NoteImageRenderer.kt 等）、`app/src/main/java/com/mynote/app/ui/export/`（导出对话框/ViewModel）；测试 `NoteImageRendererTest` / `ImageExportManagerTest` / `NoteExportViewModelTest`。**不要碰 `NoteEditScreen.kt`。**

- [ ] 统计不可读图片（文件缺失或解码失败），导出 Ready 界面提示「有 N 张图片无法读取，导出结果不含它们」；N=0 不提示。
- [ ] 相关测试补齐并保持全绿。

### Task 2: 条目 25 — 编辑页焦点链（主线程）

**Files:** Modify `app/src/main/java/com/mynote/app/ui/notes/NoteEditScreen.kt`

- [ ] `titleFocusRequester`；新建且字段为空时首帧聚焦。
- [ ] 标题 `ImeAction.Next` → 正文聚焦。
- [ ] AI 结果 apply 后 `contentFocusRequester.requestFocus()`。

### Task 3: 条目 26 — 全屏预览接线（主线程）

**Files:** Modify `NoteEditScreen.kt`

- [ ] `previewFile` 状态 + 预览图 clickable + 渲染 `NoteImagePreviewDialog`。

### Task 4: 条目 27 — 插图反馈与 GC 时机（主线程）

**Files:** Modify `NoteEditScreen.kt`、`app/src/main/java/com/mynote/app/ui/notes/NoteEditViewModel.kt`（或所在文件，用 glob 确认）、`app/src/main/java/com/mynote/app/data/image/ImageStore.kt`、`app/src/main/java/com/mynote/app/data/repository/NoteRepository.kt`

- [ ] `insertImage` 失败回调 + snackbar。
- [ ] `importAndCompress` 失败清理半成品。
- [ ] `saveNote` 图片名消失触发 GC。
- [ ] 测试：`NoteRepositoryTest` 补回收用例；`ImageStoreTest` 补清理用例。

### Task 5: 条目 28 分享文案（主线程，仅编辑页入口）

**Files:** Modify `NoteEditScreen.kt`

- [ ] 分享文案追加「（含 N 张图片，未包含在文本中）」。

### Task 6: 验证与发布

- [ ] 全量 `.\gradlew :app:testDebugUnitTest` 通过（记录新总数）。
- [ ] `app/build.gradle.kts`：`1.4.3` / `1_04_03`。
- [ ] README：版本行、测试数、产物名。
- [ ] `docs/backlog.md`：条目 25-28 → 已完成（v1.4.3）；修订记录追加。
- [ ] `.\gradlew :app:assembleRelease` 产出校验。
- [ ] 本地提交（不 push）。

---

## 修订记录

- 2026-09-14 计划创建。
- 2026-09-14 执行情况：条目 25 的「AI 结果后焦点」一并实现（编辑页行为，非 AI 助手功能改造）；条目 28 分享文案部分在主线程完成，其余由子代理完成，两者文件域不重叠。