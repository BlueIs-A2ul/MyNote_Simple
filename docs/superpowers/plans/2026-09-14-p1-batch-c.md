# MyNote P1 批次 C 实施计划（1.4.4：条目 29/30/31/32/33/34/35）

> **For agentic workers:** 配套设计文档 `docs/superpowers/specs/2026-09-14-p1-batch-c-design.md`。本批文件域交叉多，按以下并行/串行划分（冲突地图见 backlog 文末）：
> - **并行域 A（独立子代理）**：条目 30 `TimeFormat.kt` + `TimeFormatTest.kt`（纯逻辑）。
> - **主线程串行 1（数据层）**：条目 29/32 的 `NoteDao.kt`、`NoteRepository.kt`、`NotesViewModel.kt` + DAO/VM 测试（排序变体 + 防抖 + sort 透传）。
> - **主线程串行 2（列表 UI）**：条目 29/31/32 的 `NotesScreen.kt`、`NoteRow.kt`（分类名、snippet、置顶分区、缓存）+ `TrashScreen.kt`（预览）。
> - **并行域 B（独立子代理，等主线程 1 完成仓库改动后再派）**：条目 33 `ui/categories/*`、`ui/theme/NoteColors`（用色 UI）+ 测试；仓库侧 `updateCategoryColor` 由主线程在串行 1 一并提供。
> - **主线程串行 3（设置域）**：条目 34/35 `ui/settings/*`、新增 `TrashRetentionStore`、`MainActivity`/`AppContainer`/`AppNavHost`、`TrashViewModel` + 测试。
> 步骤使用 `- [ ]` 跟踪。

**Goal:** 搜索信息补齐、排序全视图生效、列表/回收站视觉与预览、搜索防抖、分类颜色可选、设置通用分区与动态取色反馈；发布 1.4.4。

**Tech Stack:** Kotlin、Compose、Room、Robolectric/JUnit4。无新依赖、无新权限、无 Room 迁移。

---

### Task 1: 条目 30 — 相对时间分钟/小时级（并行域 A）

**Files:** Modify `app/src/main/java/com/mynote/app/util/TimeFormat.kt`、`app/src/test/java/.../TimeFormatTest.kt`

- [ ] 插入「刚刚 / N 分钟前 / N 小时前」档位（<60s/<60min/<24h；跨天优先「昨天」等既有档位）。
- [ ] 补边界单测（59s、60s、59min、60min、23h、24h，跨昨天等）。

### Task 2: 条目 29+32 数据层（主线程串行 1）

**Files:** Modify `app/src/main/java/com/mynote/app/data/db/NoteDao.kt`、`NoteRepository.kt`、`ui/notes/NotesViewModel.kt`；测试 `NoteDaoTest`、`NotesViewModelTest`

- [ ] DAO：`observeByCategory` / `observeUncategorized` / `search` 增加按 Mode 分发（CREATED_DESC / TITLE_ASC 变体 @Query + 默认方法）。
- [ ] Repository：三方法透传 `sortMode`。
- [ ] VM：`query.debounce(200).distinctUntilChanged()`；全部数据源分支改用 `sortMode.value`。
- [ ] 新增 `updateCategoryColor`（条目 33 用）。
- [ ] 测试：DAO 排序变体、VM 防抖、VM 各筛选下排序生效。

### Task 3: 条目 29/31/32 列表 UI（主线程串行 2）

**Files:** Modify `NotesScreen.kt`、`ui/components/NoteRow.kt`、`ui/trash/TrashScreen.kt`；测试 `HighlightTextTest`（snippet）

- [ ] `snippetForHighlight` 纯函数 + 测试；NoteRow 搜索态用窗口摘要。
- [ ] 行内分类名：全部 tab 或搜索态显示。
- [ ] 置顶图钉 primary 色 + 「置顶/其他」分组标题。
- [ ] NoteRow `remember` 缓存 plainText/高亮；NotesScreen 分类 Map。
- [ ] 排序菜单去掉 `sortEnabled` 灰化。
- [ ] TrashScreen：行点击只读预览（标题/摘要/图片数/剩余天数）；确认框带摘要。

### Task 4: 条目 33 — 分类颜色（并行域 B，等 Task 2 完成后派发）

**Files:** `ui/categories/*`、`ui/theme/NoteColors.kt`、`CategoriesViewModelTest`；`NoteCategoryDao` 无需改（复用 update）

- [ ] 新建对话框 6 色选择（默认最少使用色）；行内色点循环换色持久化。

### Task 5: 条目 34/35 — 设置域（主线程串行 3）

**Files:** Modify `ui/settings/*`、新增 `data/settings/TrashRetentionStore.kt`、`MainActivity.kt`、`di/AppContainer.kt`、`ui/navigation/AppNavHost.kt`、`ui/trash/TrashViewModel.kt`；测试 `TrashRetentionStoreTest`、`SettingsViewModelTest`

- [ ] 默认排序三选（读/写 NoteSortStore）。
- [ ] 回收站保留期 7/30/90（TrashRetentionStore；MainActivity/TrashViewModel 用其 ttl；回收站文案显示当前值）。
- [ ] 动态取色开启时色板降透明 + 提示；点色圈关闭动态取色 + snackbar。

### Task 6: 验证与发布

- [ ] 全量单测通过；版本 1.4.4 / 1_04_04；README 同步；backlog 29-35 → 已完成（v1.4.4）+ 修订记录。
- [ ] `assembleRelease` 产出校验；本地提交（不 push）。

---

## 修订记录

- 2026-09-14 计划创建。口径拍板：排序全视图生效（#11 验收为准，#16 的禁用说明作废，见设计文档）。
- 2026-09-14 执行情况：（待填，收尾时回写实际偏差）。